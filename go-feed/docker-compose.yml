services:
  feedsim:
    build: .
    ports:
      - "8100:8100"
    environment:
      - FEED_PORT=8100
      - FEED_HOST=0.0.0.0
      - FEED_SEED=0
      - MONGO_URI=mongodb://mongo:27017/feedsim?replicaSet=rs0
      - SEND_BUFFER=4096
      # S3 Glacier archiver (uncomment and set S3_BUCKET to enable)
      # - S3_BUCKET=
      # - S3_REGION=us-east-1
      # - S3_PREFIX=feedsim
      # - ARCHIVE_INTERVAL_HOURS=6
      # - ARCHIVE_AFTER_HOURS=24
    depends_on:
      mongo:
        condition: service_healthy
    restart: unless-stopped

  mongo:
    image: mongo:8.0
    command: ["--replSet", "rs0"]
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "try { rs.status().ok } catch(e) { rs.initiate().ok }"]
      interval: 5s
      timeout: 10s
      retries: 5
    restart: unless-stopped

volumes:
  mongodata:
